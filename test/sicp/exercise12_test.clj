(ns sicp.exercise12-test
  (:require [clojure.test :refer :all]
            [sicp.exercise12 :refer :all]))

(deftest should-return-proper-result
  (testing "Exercise 1.2: Translate the following expression into prefix."
    (is (= (* -1 (/ 37 150)) (run )))))
